% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_doi_tiered.R
\name{nature_transform_backfill}
\alias{nature_transform_backfill}
\title{Apply the deterministic Nature transform (no API) into the ledger}
\usage{
nature_transform_backfill(db_path = NULL, state_path = NULL)
}
\arguments{
\item{db_path}{Corpus DuckDB. Defaults to config$db_folder/articles.duckdb}

\item{state_path}{Ledger parquet. Defaults to {pqt_patch}/nature_attempts.parquet}
}
\value{
Tibble of the ledger after applying (invisibly)
}
\description{
Writes \verb{10.1038/<id>} for every DOI-less \verb{nature.com/articles/<id>} handle of
a structurally-safe shape (see \code{nature_safe_doi}) straight into the nature
ledger, merging with any already-verified hits (deduped by Handle). Free and
instant — used when API verification budget is unavailable but the transform
is trusted for these shapes. \code{sim} is NA to mark rows as transform-derived
rather than title-verified.
}
