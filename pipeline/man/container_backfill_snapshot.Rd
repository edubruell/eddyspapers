% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_doi_snapshot.R
\name{container_backfill_snapshot}
\alias{container_backfill_snapshot}
\title{Resumable container DOI backfill from the OpenAlex snapshot}
\usage{
container_backfill_snapshot(
  db_path = NULL,
  works_glob,
  srcmap,
  state_path = NULL,
  ...
)
}
\arguments{
\item{db_path}{Corpus DuckDB. Defaults to config$db_folder/articles.duckdb}

\item{works_glob}{Local works parquet glob from \code{pull_openalex_source_works()}}

\item{srcmap}{Tibble (journal, src_id) from \code{resolve_container_sources()}}

\item{state_path}{Ledger parquet. Defaults to {pqt_patch}/openalex_attempts.parquet}

\item{...}{Guard parameters forwarded to \code{snapshot_match_container_dois()}
(\code{min_content_sim}, \code{min_raw_sim}, \code{min_content_tokens}, \code{prefix_min_share},
\code{year_tol})}
}
\value{
Tibble of this run's new hits (invisibly)
}
\description{
Matches DOI-less container handles to the local works subset and merges the
hits into the \code{openalex_attempts.parquet} ledger (the same ledger the API
route would fill), so \code{finalize_doi_patch()} folds them under provenance
\code{openalex}. Idempotent: existing ledger handles are kept, new ones appended.
}
