% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/openalex.R
\name{nature_backfill_openalex}
\alias{nature_backfill_openalex}
\title{Resumable Nature DOI backfill, verified in batches via OpenAlex}
\usage{
nature_backfill_openalex(
  db_path = NULL,
  mailto,
  limit = Inf,
  state_path = NULL,
  min_sim = 0.4,
  batch = 50L
)
}
\arguments{
\item{db_path}{Corpus DuckDB. Defaults to config$db_folder/articles.duckdb}

\item{mailto}{Polite-pool contact (required)}

\item{limit}{Max handles to attempt this run}

\item{state_path}{Ledger parquet. Defaults to {pqt_patch}/nature_attempts.parquet}

\item{min_sim}{Minimum title similarity to accept a verified DOI}

\item{batch}{DOIs per OpenAlex request (max 50)}
}
\value{
Tibble of this run's attempts (invisibly)
}
\description{
Transforms each \verb{nature.com/articles/<id>} to \verb{10.1038/<id>}, then verifies
in batches of up to \code{batch} DOIs against OpenAlex (existence + title
similarity). Ledger schema matches \code{nature_backfill} (Handle, doi, sim,
attempted_at) so its hits and this run's are interchangeable at finalize.
}
