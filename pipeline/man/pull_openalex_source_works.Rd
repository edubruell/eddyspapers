% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_doi_snapshot.R
\name{pull_openalex_source_works}
\alias{pull_openalex_source_works}
\title{Pull OpenAlex works for given source ids into local parquet}
\usage{
pull_openalex_source_works(
  source_ids,
  out_dir,
  files_per_chunk = 60L,
  deadline_s = 300,
  retries = 8L,
  threads = 4L,
  con = NULL
)
}
\arguments{
\item{source_ids}{Character vector of OpenAlex source ids to keep}

\item{out_dir}{Directory for per-chunk parquet files}

\item{files_per_chunk}{Max part files per chunk (default 60)}

\item{deadline_s}{Per-attempt watchdog deadline in seconds (default 300)}

\item{retries}{Max attempts per chunk (default 8)}

\item{threads}{DuckDB threads per worker (default 4)}

\item{con}{Optional DuckDB connection for the file listing}
}
\value{
\code{out_dir} (invisibly)
}
\description{
The works snapshot (~510M rows / 725 GB) is partitioned by \code{updated_date},
not venue, so there is no cheap prefix filter: every partition is scanned,
projecting the \code{primary_location.source.id} leaf and keeping only
\code{article}/\code{review} rows in the given sources. The scan runs through the shared
stall-proof watchdog runner (\code{oa_pull_chunks}) — part-file chunking, a
short-lived processx worker per chunk, atomic rename, and hard-kill/retry on
the dead-socket S3 stall — the same machinery Track B's metrics pull uses.
Completed chunks are skipped on resume.
}
