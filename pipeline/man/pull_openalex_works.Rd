% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_doi_snapshot.R
\name{pull_openalex_works}
\alias{pull_openalex_works}
\title{Pull OpenAlex works for given source ids into local parquet}
\usage{
pull_openalex_works(source_ids, out_dir, years = NULL, con = NULL)
}
\arguments{
\item{source_ids}{Character vector of OpenAlex source ids to keep}

\item{out_dir}{Directory for per-chunk parquet files}

\item{years}{Update-years to sweep. Defaults 2016..current}

\item{con}{Optional DuckDB connection}
}
\value{
\code{out_dir} (invisibly)
}
\description{
The works snapshot (~510M rows / 725 GB) is partitioned by \code{updated_date},
not venue, so there is no cheap prefix filter: a single projected scan reads
the \code{primary_location.source.id} leaf across every partition. To bound the
blast radius of a network failure the scan is chunked by update-month; each
chunk writes its own file and is skipped on resume, so a failed month re-runs
alone. Only \code{article}/\code{review} rows in the given sources are kept.
}
