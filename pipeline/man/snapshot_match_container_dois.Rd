% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_doi_snapshot.R
\name{snapshot_match_container_dois}
\alias{snapshot_match_container_dois}
\title{Match DOI-less container papers to local OpenAlex works}
\usage{
snapshot_match_container_dois(
  db_path,
  works_glob,
  srcmap,
  min_content_tokens = 3L,
  prefix_min_share = 0.05,
  year_tol = 1L,
  fuzzy = FALSE,
  min_content_sim = 0.9
)
}
\arguments{
\item{db_path}{Corpus DuckDB (read-only)}

\item{works_glob}{Local works parquet glob from \code{pull_openalex_source_works()}}

\item{srcmap}{Tibble (journal, src_id) from \code{resolve_container_sources()}}

\item{min_content_tokens}{Min content words in the shorter title (default 3)}

\item{prefix_min_share}{Min share of a source's DOIs a prefix must cover to
be trusted (default 0.05)}

\item{year_tol}{Allowed |year difference| (default 1)}

\item{fuzzy}{Also run the token-blocked containment stage (default FALSE)}

\item{min_content_sim}{Min content-token containment for the fuzzy stage
(default 0.9)}
}
\value{
Tibble (Handle, doi, content_sim, match_type) — one row per handle
}
\description{
For every DOI-less corpus paper whose journal resolved to an OpenAlex source,
matches it to a same-source, near-same-year work under a multi-signal guard
deliberately STRICTER than the API route's \code{title_sim >= 0.5} (the snapshot
join sees \emph{every} work in a journal-year, and OpenAlex sometimes attaches a
foreign DOI to a work — a pediatric DOI on an APSR record, PLoS on Energy
Economics). Two stages:
\itemize{
\item \strong{exact} (always): a hash equi-join on the sorted content-token
SET — robust to word order/stopwords, near-linear, captures the bulk;
\item \strong{fuzzy} (opt-in via \code{fuzzy = TRUE}): a token-blocked containment
join over the residual, keeping shorter-in-longer matches (OpenAlex
dropping a subtitle) while rejecting different papers that merely share
stopwords/years.
}
Both stages require: the shorter title has >= \code{min_content_tokens} content
words (kills generic titles like "Introduction"/"Reply"); and the DOI's
registrant prefix covers >= \code{prefix_min_share} of that source's DOIs (kills
OpenAlex's foreign-DOI noise, data-driven).
}
