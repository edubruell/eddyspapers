% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_doi_snapshot.R
\name{doi_less_journals}
\alias{doi_less_journals}
\title{DOI-less non-series journals in the corpus (widened past publisher hosts)}
\usage{
doi_less_journals(db_path, min_papers = 20L)
}
\arguments{
\item{db_path}{Corpus DuckDB (read-only)}

\item{min_papers}{Keep journals with at least this many such papers. The tail
of tiny journals resolves noisily and adds little recall; default 20}
}
\value{
Tibble (journal, n) ordered by descending count
}
\description{
All journals (with paper counts) of DOI-less, \strong{non-series} articles carrying
a title and year — the Phase-B title-match population. Unlike
\code{container_doi_less_journals()} there is no host filter: working-paper series
(\code{is_series}) are the only exclusion, since they are ~never DOI'd. Nearly all
of these journals resolve to OpenAlex sources, so the title matcher is not
limited to the eight publisher hosts Track A targeted.
}
