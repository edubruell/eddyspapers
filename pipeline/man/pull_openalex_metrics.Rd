% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_openalex.R
\name{pull_openalex_metrics}
\alias{pull_openalex_metrics}
\title{Pull OpenAlex work metrics for the corpus DOIs into local parquet}
\usage{
pull_openalex_metrics(
  dois_path,
  out_dir,
  files_per_chunk = 60L,
  deadline_s = 300,
  retries = 8L,
  threads = 4L,
  con = NULL
)
}
\arguments{
\item{dois_path}{Corpus DOI parquet from \code{export_corpus_dois()}}

\item{out_dir}{Directory for per-chunk parquet files}

\item{files_per_chunk}{Max part files per chunk (default 60)}

\item{deadline_s}{Per-attempt watchdog deadline in seconds (default 300)}

\item{retries}{Max attempts per chunk (default 8)}

\item{threads}{DuckDB threads per worker (default 4)}

\item{con}{Optional DuckDB connection for the file listing}
}
\value{
\code{out_dir} (invisibly)
}
\description{
Sweeps the whole \code{works} snapshot (partitioned by \code{updated_date}), projecting
the flat metric columns and keeping only rows whose DOI is in the corpus set.
Each part-file chunk runs in a short-lived \code{processx} worker; the parent polls
for the chunk's atomically-renamed output and, on a \code{deadline_s} timeout,
hard-kills the worker and retries on a fresh process WITHOUT waiting for it to
be reaped — httpfs S3 reads intermittently stall on a dead TCP connection that
neither \code{http_timeout} nor SIGKILL clears promptly, and waiting for the reap
is what turned earlier pulls into multi-hour hangs. Completed chunks are
skipped on resume.
}
