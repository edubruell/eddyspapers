% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_openalex.R
\name{build_article_openalex}
\alias{build_article_openalex}
\title{Build the \code{article_openalex} rows from the pulled metrics}
\usage{
build_article_openalex(meta_glob, db_path = NULL)
}
\arguments{
\item{meta_glob}{Local metrics parquet glob from \code{pull_openalex_metrics()}}

\item{db_path}{Corpus DuckDB. Defaults to config$db_folder/articles.duckdb}
}
\value{
Tibble of \code{article_openalex} rows (one per handle)
}
\description{
Joins every DOI'd corpus paper to its OpenAlex work by DOI. A DOI can occur
in several \code{updated_date} partitions (the giant re-dumps overlap the daily
ones); the freshest row (max \code{updated_date}) wins. A DOI can also map to
several corpus handles (paper versions / duplicate RePEc handles) — each
handle gets that work's metrics, keyed by handle.
}
