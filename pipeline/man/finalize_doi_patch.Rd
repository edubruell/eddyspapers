% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_doi_tiered.R
\name{finalize_doi_patch}
\alias{finalize_doi_patch}
\title{Build one DOI update_columns patch from every source}
\usage{
finalize_doi_patch(db_path = NULL, rds_folder = NULL, pqt_patch_folder = NULL)
}
\arguments{
\item{db_path}{Corpus DuckDB. Defaults to config$db_folder/articles.duckdb}

\item{rds_folder}{RDS cache. Defaults to config$rds_folder}

\item{pqt_patch_folder}{Patch output. Defaults to config$pqt_patch_folder}
}
\value{
Manifest path (invisibly), or NULL when nothing to patch
}
\description{
Folds local extraction (RDS \code{redif}, \code{url}) and every attempt ledger
(\code{nature_transform}, \code{openalex}, \code{crossref_container}, blind \code{crossref}) into
a single patch, deduped by Handle with provenance priority
redif > url > nature_transform > openalex > crossref_container > crossref.
Authoritative sources (redif/url) may correct a stale DOI; the fuzzy ledgers
only fill doi-less handles. Rows already
carrying the same DOI are dropped so re-runs stay minimal. This is what
rescues the interrupted blind run's 571 hits (never written to a patch).
}
