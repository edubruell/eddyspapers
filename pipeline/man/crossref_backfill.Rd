% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_doi.R
\name{crossref_backfill}
\alias{crossref_backfill}
\title{Resumable Crossref DOI backfill}
\usage{
crossref_backfill(
  db_path = NULL,
  mailto,
  limit = 500,
  include_series = FALSE,
  state_path = NULL,
  delay_secs = 0.4
)
}
\arguments{
\item{db_path}{Path to DuckDB database. Defaults to config$db_folder/articles.duckdb}

\item{mailto}{Contact email for the polite pool (required)}

\item{limit}{Max handles to attempt this run}

\item{include_series}{Also attempt working-paper rows (default FALSE)}

\item{state_path}{Attempts parquet. Defaults to {pqt_patch_folder}/crossref_attempts.parquet}

\item{delay_secs}{Sleep between requests (default 0.4 ≈ 2.5 req/s)}
}
\value{
Tibble of this run's attempts (invisibly)
}
\description{
Works through DOI-less articles (published articles first — WP series have
near-zero mint rates), one polite request per handle, recording every
attempt (hit or miss) in a state parquet so re-runs never re-query. Emits an
update_columns patch (doi_source 'crossref') for the hits of this run.
}
\details{
Run a calibration sample first (limit = 500), hand-check the emitted
attempts file, adjust EDDY_CROSSREF_SCORE_MIN if needed, then run with
limit = Inf overnight (~3 req/s polite ceiling is respected via delay).
}
