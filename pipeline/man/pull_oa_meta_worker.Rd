% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_openalex.R
\name{pull_oa_meta_worker}
\alias{pull_oa_meta_worker}
\title{Pull one works-part-file chunk's metrics into a local parquet}
\usage{
pull_oa_meta_worker(globs, out, dois_path, threads = 4L)
}
\arguments{
\item{globs}{Character vector of S3 parquet globs/paths}

\item{out}{Destination parquet path}

\item{dois_path}{Corpus DOI parquet from \code{export_corpus_dois()}}

\item{threads}{DuckDB threads (fewer = fewer concurrent S3 connections =
fewer dead-socket stalls). Default 4}
}
\value{
\code{out} (invisibly)
}
\description{
Scans the given part-file globs over httpfs, projects the flat metric columns
(\code{oa_metrics_select_sql}), keeps only rows whose DOI is in \code{dois_path}, and
writes to \code{out} via a \code{.tmp} + atomic rename so an interrupted scan never
leaves a half file that looks complete. This is the in-process form used in
tests; \code{pull_openalex_metrics} runs the same SQL in a watchdog subprocess.
}
