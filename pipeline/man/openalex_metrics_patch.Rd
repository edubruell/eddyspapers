% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_openalex.R
\name{openalex_metrics_patch}
\alias{openalex_metrics_patch}
\title{Write the \code{article_openalex} replace-table patch}
\usage{
openalex_metrics_patch(meta_glob, db_path = NULL, pqt_patch_folder = NULL)
}
\arguments{
\item{meta_glob}{Local metrics parquet glob from \code{pull_openalex_metrics()}}

\item{db_path}{Corpus DuckDB. Defaults to config$db_folder/articles.duckdb}

\item{pqt_patch_folder}{Patch folder. Defaults to config$pqt_patch_folder}
}
\value{
Path to the patch manifest (invisibly)
}
\description{
Builds the table and emits a \code{replace_table} patch (schema comes from the
parquet; \code{migrate_schema} restores the indices after apply). Ship it with the
standard backfill flow: \code{run_backfill()} (apply + baseline dump) then
\code{deploy_patches.sh}.
}
